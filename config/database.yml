# PostgreSQL. Versions 9.5 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/opt/homebrew/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # `pool`, not `max_connections`. Rails has no `max_connections` key: the value went into a
  # setting nothing reads, the pool silently fell back to Rails' hardcoded default of 5, and
  # RAILS_MAX_THREADS did nothing at all. Verified at runtime — `configuration_hash[:pool]` was
  # nil while `connection_pool.size` reported 5.
  #
  # It happened to work only because Puma also defaults low: config/puma.rb takes 3 threads from
  # the same variable, and 3 threads fit in 5 connections. Raising RAILS_MAX_THREADS would have
  # given Puma more threads without giving them anywhere to connect, and the extra threads would
  # have blocked on ActiveRecord::ConnectionTimeoutError under load.
  #
  # One variable now drives both, which is what makes the two move together.
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>


# Credentials come from the environment in every environment, with no fallback
# (S1 / P0-SEC-03). development and test previously hardcoded postgres/postgres — the
# shared literal committed across the platform — and this file was gitignored, so two
# developers could not produce the same build. `fetch` raises a KeyError naming the
# missing variable rather than connecting as a default superuser.
development:
  <<: *default
  database: <%= ENV.fetch("DB_NAME") { "kinetix_warehouse_dev" } %>
  username: <%= ENV.fetch("DB_USERNAME") %>
  password: <%= ENV.fetch("DB_PASSWORD") %>
  host: <%= ENV.fetch("DB_HOST") { "localhost" } %>
  port: <%= ENV.fetch("DB_PORT") { 5432 } %>

test:
  <<: *default
  database: <%= ENV.fetch("TEST_DB_NAME") { "kinetix_warehouse_test" } %>
  username: <%= ENV.fetch("DB_USERNAME") %>
  password: <%= ENV.fetch("DB_PASSWORD") %>
  host: <%= ENV.fetch("DB_HOST") { "localhost" } %>
  port: <%= ENV.fetch("DB_PORT") { 5432 } %>

production:
  primary: &primary_production
    <<: *default
    database: <%= ENV.fetch("DB_NAME") { "kinetix_warehouse_production" } %>
    username: <%= ENV.fetch("DB_USERNAME") %>
    password: <%= ENV.fetch("DB_PASSWORD") %>
    host: <%= ENV.fetch("DB_HOST") { "localhost" } %>
    port: <%= ENV.fetch("DB_PORT") { 5432 } %>
  cache:
    <<: *primary_production
    database: kinetix_warehouse_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: kinetix_warehouse_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: kinetix_warehouse_production_cable
    migrations_paths: db/cable_migrate
